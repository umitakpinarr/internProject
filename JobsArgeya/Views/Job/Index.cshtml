@{
    ViewData["Title"] = Localizer.Get("home_breadcrumb_title");
    ViewData["BreadcrumbTitle"] = Localizer.Get("job_breadcrumb_title");
}
@model ApplyModel

@if (TempData["successMessage"] != null)
{
    <div class="alert alert-success" role="alert">
        @TempData["successMessage"]
    </div>
}
@if (TempData["dangerMessage"] != null)
{
    <div class="alert alert-danger" role="alert">
        @TempData["dangerMessage"]
    </div>
}

<section class="tools">
    <div class="container">
        <div class="t-row">
            <div class="d-table-cell">
                <a class="btn btn-primary" style="margin-top:25px; margin-bottom:25px; background-color:@ViewData["SiteColor"]; border-color:@ViewData["SiteColor"];" href="/@Context.Request.RouteValues["culture"]" title="@Localizer.Get("job_onlycv_back")">
                    <span style="line-height: 12px; font-size: 20px;">&#x2190;</span> @Localizer.Get("job_onlycv_back")
                </a>
            </div>
        </div>
    </div>
</section>
<div class="container shadow-sm p-3 mb--20 bg-body rounded">
    <div class="title text-center">
        <h1>@Localizer.Get("job_onlycv_title")</h1>
        <p>
            @Localizer.Get("job_onlycv_desc")
        </p>
    </div>
  
    <div class="job-apply-form">
        <form action="/Job" method="post" enctype="multipart/form-data">
            <div class="form-group" style="margin-bottom:10px;">
                <label for="FullName"><b>@Localizer.Get("job_form_fullname")</b></label>
                <input name="FullName" id="FullName" class="form-control" required>
            </div>
            <div class="form-group" style="margin-bottom:10px;">
                <label for="Phone"><b>@Localizer.Get("job_form_phone")</b></label>
                <input name="Phone" id="Phone" class="form-control" placeholder="05XXXXXXXXX" required>
            </div>
            <div class="form-group" style="margin-bottom:10px;">
                <label for="Email"><b>@Localizer.Get("job_form_email")</b></label>
                <input type="email" name="Email" id="Email" class="form-control" required>
            </div>
            <div class="form-group" style="margin-bottom:10px;">
                <label for="Gender"><b>@Localizer.Get("job_form_gender")</b></label>
                <select name="Gender" id="Gender" class="form-control" required>
                    <option value="">@Localizer.Get("gender_nonselected")</option>
                    <option value="Belirtilmemiş">@Localizer.Get("gender_nonspecify")</option>
                    <option value="Erkek">@Localizer.Get("gender_male")</option>
                    <option value="Kadın">@Localizer.Get("gender_female")</option>
                </select>
            </div>
            <div class="form-group" style="margin-bottom:10px;">
                <label for="University"><b>@Localizer.Get("job_form_university")</b></label>
                <input name="University" id="University" class="form-control" required>
            </div>
            <div class="form-group" style="margin-bottom:10px;">
                <label for="Faculty"><b>@Localizer.Get("job_form_faculty")</b></label>
                <input name="Faculty" id="Faculty" class="form-control" required>
            </div>
            <div class="form-group" style="margin-bottom:10px;">
                <label for="Resume"><b>@Localizer.Get("job_form_resume")</b></label>
                <textarea name="Resume" id="Resume" class="form-control" required></textarea>
            </div>
            <div class="d-flex flex-row">
                <div class="form-group mr-auto p-2" style="margin-bottom:10px;">
                    <label for="FormFile"><b>@Localizer.Get("job_form_upload")</b></label>
                    <input type="file" name="FormFile" id="FormFile" class="form-control-file" accept="application/pdf" required>
                </div>
                <div class="form-group p-2" style="margin-bottom:10px;">
                    <img style="display: inline-block; width: 180px; height: 50px; " id="imgCaptcha" src="/argeya-captcha?@DateTime.Now.ToString("ddMMyyyyHHmmsss")" />
                    <a class="btn btn-sm small gray" style="display: inline-block; padding: 8px; cursor: pointer; vertical-align: top; background-color:@ViewData["SiteColor"]; border-color:@ViewData["SiteColor"]; color: #fff !important; text-decoration: none;" onclick="var today = new Date(); document.getElementById('imgCaptcha').src = '/argeya-captcha?r=' + today; return false;">
                        <i class="fa fa-refresh" aria-hidden="true" style="font-size: 30px;"></i>
                    </a>
                    <input type="text" name="captcha" id="captcha" required />
                </div>
            </div>
            <div class="container text-center">
                <button type="submit" class="btn btn-primary btn-lg active w-75" style="background-color:@ViewData["SiteColor"]; border-color:@ViewData["SiteColor"];">@Localizer.Get("job_onlycv_submit")</button>
            </div>
        </form>
    </div>
</div>